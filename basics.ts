Math.round(3.14);

const myVar: Number = 54;
let MovieTitle: string = "Mahan";
MovieTitle = 54;
MovieTitle.toUpperCase();

let catNums: number = 9;
catNums += 1;

let gameOver: boolean = false;
gameOver = true;
gameOver = "true";

let thing: any = "hello";
thing = 1;
thing = false;
thing = "bye";

const movies = ["Prestige", "Aliens", "Note book", "Oppenhimer"];
let foundMovie: string;

for (let movie of movies) {
  if (movie === "Prestige") {
    foundMovie = "Prestige";
  }
}
